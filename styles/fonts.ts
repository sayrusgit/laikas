import { DM_Mono, Poppins } from 'next/font/google';

export const poppins = Poppins({ subsets: ['latin-ext'], weight: ['400', '500', '700'] });
export const dm_mono = DM_Mono({ subsets: ['latin-ext'], weight: ['400', '500'] });
